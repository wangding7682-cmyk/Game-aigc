# 角色占位到主项目映射

## 目的
这份文档把准备层中的角色占位规则，映射到主项目当前已经存在的 3D 角色实现位置。

当前只做映射，不改主项目代码。

---

## 1. 准备层来源

本映射回指以下准备层文件：

1. `01_character_placeholders/MVP角色占位工程化说明.md`
2. `01_character_placeholders/status-matrix.md`
3. `01_character_placeholders/weakpoint-and-focus-points.md`
4. `01_character_placeholders/material-switch-rules.md`
5. `01_character_placeholders/character-readability-checklist.md`

---

## 2. 主项目目标文件

### 当前主落点

1. `狙击外星人升级版/scripts/pve/pve_target_controller_3d.gd`
2. `狙击外星人升级版/scenes/pve/pve_target_actor.tscn`

### 当前主项目已存在的角色结构

在 `pve_target_controller_3d.gd` 中，已经能找到这些结构节点：

1. `BodyMesh`
2. `HeadMesh`
3. `ShoulderLeft`
4. `ShoulderRight`
5. `CostumeMesh`
6. `AccentMesh`
7. `WeakpointMesh`
8. `HaloMesh`
9. `EyeLeft`
10. `EyeRight`

说明：

- 准备层的最小结构，已经和主项目现状基本对上

---

## 3. 状态矩阵映射

| 准备层状态 | 主项目映射位置 | 当前主项目对应变量 / 条件 | 后续接入动作 |
|---|---|---|---|
| `alien_base_disguise` | `pve_target_controller_3d.gd` | `actor_kind == "target"` + 基础 `disguise_strength` | 保持为默认外星人入口态 |
| `alien_costume_disguise` | `pve_target_controller_3d.gd` | `disguise_strength` 较高，`CostumeMesh` 常开 | 后续把服装化伪装参数单独配置化 |
| `alien_moving_profile` | `pve_target_controller_3d.gd` | `behavior_type == "moving"` | 保持由行为类型驱动 |
| `alien_scan_highlight` | `pve_target_controller_3d.gd` | `highlighted_until` + `HaloMesh` | 后续把扫描高亮强度参数外置 |
| `alien_weakpoint_open` | `pve_target_controller_3d.gd` | `behavior_type == "weakpoint"` / `tutorial_primary` | 后续把弱点窗口规则统一到行为工厂 |
| `civilian_calm_idle` | `pve_target_controller_3d.gd` | `actor_kind == "civilian"` 默认态 | 保持为平民主入口态 |
| `civilian_moving` | `pve_target_controller_3d.gd` | `actor_kind == "civilian"` + `behavior_type == "moving"` | 继续沿用 |
| `civilian_false_clue` | `pve_target_controller_3d.gd` | `false_clue_active` / `false_clue_until` | 后续把假线索配置集中化 |

---

## 4. 点位映射

| 准备层点位 | 主项目当前落点 | 当前状态 | 后续接入动作 |
|---|---|---|---|
| `HitFocus` | `get_impact_focus_point()` | 已存在 | 后续如接入正式模型，改为挂点优先 |
| `WeakPoint` | `WeakpointMesh` | 已存在 | 后续可拆成独立节点或挂点 |
| `ScanHighlight` | `HaloMesh` | 已存在 | 后续支持世界空间扫描增强 |
| `ReviewFocus` | 当前未显式节点，依赖 `get_impact_focus_point()` 和头胸区域 | 部分存在 | 后续建议单独挂点化 |
| `HeadLook` | `HeadMesh` / 眼部区域 | 部分存在 | 后续建议单独挂点化 |

---

## 5. 材质规则映射

| 准备层材质层 | 主项目当前变量 |
|---|---|
| `body_material` | `material` |
| `head_material` | `head_material` |
| `costume_material` | `costume_material` |
| `accent_material` | `accent_material` |
| `special_material` | `weakpoint_material` + `halo_material` |

说明：

- 主项目当前已经具备 MVP 材质切换的主要承载层
- 这意味着准备层的材质规则可以直接映射到当前脚本，不需要重新设计变量体系

---

## 6. 当前不做的事情

本轮映射明确不做：

1. 不把准备层文件自动并入主项目脚本
2. 不重构 `pve_target_controller_3d.gd`
3. 不新增正式角色模型资源
4. 不新增复杂骨骼或动画系统

---

## 7. 后续真正接入时的最小动作

当后面允许真正接入主项目时，角色侧建议按下面最小顺序推进：

1. 先把 `status-matrix.md` 转成配置表
2. 再把 `material-switch-rules.md` 转成参数表
3. 再把 `weakpoint-and-focus-points.md` 映射成显式挂点
4. 最后再考虑正式模型替换

---

## 8. 当前映射结论

角色这条线的结论是：

**准备层里的第一批规则，已经和主项目当前的 `pve_target_controller_3d.gd` 高度对口。**

也就是说，后面一旦要正式接入，角色线不需要推翻当前实现，只需要把：

1. 状态
2. 材质规则
3. 点位规则

从文档规则转换成显式配置和挂点即可。
