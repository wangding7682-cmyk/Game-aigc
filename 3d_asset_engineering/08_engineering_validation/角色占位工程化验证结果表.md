# 角色占位工程化验证结果表

## 说明
本表是 **第一轮工程化静态验证结果**。  
判断依据来自当前主项目代码与已落地的 MVP 占位结构，不代表已经完成 Godot 运行实测。

结论状态说明：

1. `静态通过`：代码与结构已明确存在
2. `基本通过`：主干已存在，但还需要运行时观察
3. `待运行验证`：规则和结构已具备，但必须进入场景确认体感
4. `未通过`：当前代码层未形成

---

| 验证项 | 当前状态 | 当前依据 | 结论 |
|---|---|---|---|
| 外星人与平民已有独立身份分支 | 静态通过 | `actor_kind == "target"` / `actor_kind == "civilian"` | 已具备基础分流 |
| 外星人服装化伪装层存在 | 静态通过 | `CostumeMesh` 已存在 | 可进入伪装可读性验证 |
| 外星人红眼结构存在 | 静态通过 | `EyeLeft` / `EyeRight` 已存在 | 可进入近中景识别验证 |
| 外星人弱点结构存在 | 静态通过 | `WeakpointMesh` 已存在 | 可进入窗口态验证 |
| 外星人扫描高亮结构存在 | 静态通过 | `HaloMesh` 已存在 | 可进入扫描可读性验证 |
| 平民假线索逻辑存在 | 静态通过 | `false_clue_profile` / `false_clue_active` / `trigger_false_clue()` 已存在 | 可进入误导强度验证 |
| 移动态存在 | 静态通过 | `behavior_type == \"moving\"` 已存在 | 可进入动态识别验证 |
| 目标与平民材质层已分开 | 静态通过 | `material / head_material / costume_material / accent_material / weakpoint_material / halo_material` 已存在 | 可做状态材质切换验证 |
| 外星人服装化伪装不会完全遮蔽关键识别点 | 基本通过 | 服装层与眼部、弱点、肩线结构并存 | 需实际跑中景验证 |
| 异常肩线是否足够稳定可读 | 待运行验证 | `ShoulderLeft / ShoulderRight` 已做不对称处理 | 需看近中景体感 |
| 红眼在中景是否仍可读 | 待运行验证 | 结构已在，但未做场景内距离验证 | 需实跑 |
| 平民假线索是否不过界 | 待运行验证 | 逻辑存在，强度尚未通过实跑确认 | 需实跑 |
| 弱点开启是否像“可击中窗口” | 基本通过 | `behavior_type == \"weakpoint\"` + `WeakpointMesh` 显隐存在 | 需实跑确认时机感 |
| 误判复盘是否已有基础焦点支持 | 基本通过 | `get_impact_focus_point()` 已存在 | 后续建议跑复盘镜头验证 |

---

## 当前静态结论

### 已成立

1. 角色占位从“纯胶囊体”升级成了结构化 MVP 占位
2. 外星人 / 平民 / 弱点 / 扫描 / 假线索五条核心线都已进入代码结构
3. 已经具备进入运行时角色可读性验证的前提

### 还不能直接下结论的

1. 中景红眼是否足够读得清
2. 肩线异常是否真的能支撑判断
3. 平民假线索是否会过强

---

## 进入下一轮的最小动作

后续只需针对角色线跑 3 类运行验证：

1. 近 / 中景身份区分
2. 扫描与弱点窗口识别
3. 平民假线索强度控制
